# spring-boot-websocket-server
A modified version of Spring.io Websocket Example  
Works as a server for EmberJS chat client, also included an example rest controller which can be used to send messages to websocket clients.

# How to run
You can use maven goal to run the application  
```
mvn spring-boot:run
```
  
